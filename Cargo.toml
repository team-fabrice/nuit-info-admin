[package]
name = "nuit-info-admin"
version = "0.1.0"
edition = "2018"

[dependencies]
diesel = { version = "1.4.8", features = ["uuid", "chrono", "postgres"] }
rocket = "0.5.0-rc.1"
rocket_sync_db_pools = { version = "0.1.0-rc.1", features = ["diesel_postgres_pool"] }
